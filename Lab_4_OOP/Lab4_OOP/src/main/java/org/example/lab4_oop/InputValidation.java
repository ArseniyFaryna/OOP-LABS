package org.example.lab4_oop;

public interface InputValidation {
    boolean isValidBrand(String brand);
    boolean isValidYear(int year);
    boolean isValidEngineVolume(double engineVolume);
    boolean isValidMarket(String market);
    boolean isValidPrice(double price);
    boolean isValidMileage(double mileage);
    boolean isValidConditionRate(double condition);
}
